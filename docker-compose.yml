version: '3.1'

services:
  redis:
    image: redis:latest
    # volumes:
    #   - /data:/data
    # command: redis-server --save 60 1 --loglevel warning
    ports:
      - "6379:6379"
  back:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - redis